import styled from "styled-components";

const H2 = styled.h2`
  font-size: 1.3rem;
  font-weight: normal;
  color: ${({ theme }) => theme.palette.secondary};
  margin: 1rem 0 2rem 0;
`;

export default H2;
